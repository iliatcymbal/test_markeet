import { Navigation } from '../navigation/Navigation';

import './header.scss';

export const Header = () => (
  <header className="header">
    <strong><a href="/">Easy people</a></strong>
    <Navigation isLogin={false} />

    isLogin && div
  </header>
);
